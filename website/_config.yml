title: "InsertResumeTitleHere"
description: "InsertResumeTitleHere"
theme: minima
markdown: kramdown
exclude: [vendor, node_modules]

# If you use a custom domain like https://badge-box.com, set:
#   url: "https://badge-box.com"
#   baseurl: ""   # empty string means site is served at the root
url: "https://badge-box.com"
baseurl: ""

# If someone instead uses a project site (e.g. https://<user>.github.io/<repo>/),
# they should change url/baseurl to something like:
#   url: "https://<user>.github.io"
#   baseurl: "/<repo>"   # e.g. "/badgebox-resume"
